#include<iostream>
using namespace std;
/*ab ek example dekho .... for example ek doctor hai
jiske paas 4 hospitals hai H1 H2 H3 H4 , H1 main 
beds hai bed a,b,c and H2 main hai a1,b1,c1 ,H3 
main a2,b2,c2 and H4 main a3,b3,c3 and for example
3 patients aaye H1 main par bas a ,b1,c3 khaali 
hai ... toh doctor ne patients ko bola ki tum
a bed se sona shuru kardo and tumhare agle patient
ke bed ka address apne paas rakhlo toh doctor
patient ko check karne ke baad address dekhke agle
patient ke paas chala jayega*/
/*array contiguous memory main save karta hain but linked
lists alag hote hai. 2 consecutives memory agar hogi
jisme pehli main data hoga and dusri main agle data ka
address hoga toh usse hum node kehte hai. aur inn nodes
ke collection ko hum linked list kehte hai
array hum faster traversal ke liye use karte hai and 
linked list ka benefit hai ki hum uss list ko extend
kar sakte hai. it is like a chain that u can remove the
iron segment from the chain and the add a new one or 
u can just add a new iron segment in the end.array main
we have to keep data elements in contiguous memory par 
linked list main we dont have to do that while deletion
or insertion.linked list ki ek aur disadvantage hai 
ki extra memory use karta hai pointer save karne ke liye
random access like arrays is not allowed ... what i
mean is that you cant access data if u know the address
of first element*/

int main(){
    
    return 0;
}